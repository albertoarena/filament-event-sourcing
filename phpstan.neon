includes:
    - vendor/larastan/larastan/extension.neon

parameters:
    level: 6
    paths:
        - src
    tmpDir: build/phpstan
    ignoreErrors:
        # Concern traits are public API consumed by downstream models, never used within src itself.
        - identifier: trait.unused
